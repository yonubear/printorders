{
  "name": "printorders",
  "version": "1.0.0",
  "description": "Print Orders App for Nextcloud",
  "main": "index.js",
  "scripts": {
    "build": "npm run build",
    "test": "npm run test"
  },
  "dependencies": {
    "browserslist-config": "^latest_version", // Update to a valid version or find an alternative
    "rollup-plugin-terser": "^latest_version",
    "workbox-google-analytics": "^latest_version",
    "inflight": "^latest_version",
    "source-map-resolve": "^latest_version",
    "sourcemap-codec": "^latest_version",
    "stable": "^latest_version",
    "resolve-url": "^latest_version",
    "source-map-url": "^latest_version",
    "urix": "^latest_version"
  },
  "devDependencies": {
    "eslint": "^latest_version",
    "jest": "^latest_version",
    "webpack": "^latest_version"
  }
}